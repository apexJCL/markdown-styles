# Markdown Styles

NPM Commands:

+ `web`: HTTP server with style showcasing
+ `dist`: Rebuilds scss files
+ `dev`: Runs an HTTP live server with Sass file watcher

Run these commands with `npm run \<command\>`
